1. Object Oriented:
```
Class Person():
  pass
  
person = Person()

添加方式1:
first_key = 
second_key = 
setattr = (person, first_key, second_key)
添加方式2:
person_info = {"first":"Corey", "last":"Robinson"}
for key, value in person_info.items():
  setattr(person, key, value)
  
conclusion: python 的class的属性并不非要在class里面定义，也可以外部添加。

2. Input
username = input("Username: ")
password = input("Password: ")

平时我们输入的信息会在屏幕上显示。
所以当我们把它换成：
import getpass
password = getpass("Password: ")
我们输入的信息是不会显示出来的。




